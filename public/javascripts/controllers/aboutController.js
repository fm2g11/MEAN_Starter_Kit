(function ( angular ) {
  var mod = angular.module( 'aboutController', [] );

  mod.controller( 'AboutCtrl', function ( $scope, $log ) {
    $log.log( 'AboutCtrl invoked' );
    
  } );

})( angular );